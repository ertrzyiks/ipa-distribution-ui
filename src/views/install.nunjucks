{% extends "layout.nunjucks" %}

{% block content %}
  <h2 class="ui header">{{bundle.name}} - {{bundle.version}}</h2>

  <a href="{{bundle.download_url}}" class="ui primary button">
    <i class="cloud download icon"></i> Install
  </a>
{% endblock %}
